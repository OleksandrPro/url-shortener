[project]
name = "url-shortener"
version = "0.1.0"
description = "Training project. Just a url shortener"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.0",
]
